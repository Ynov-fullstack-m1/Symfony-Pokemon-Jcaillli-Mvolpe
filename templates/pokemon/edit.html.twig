{% extends 'base.html.twig' %}

{% block title %}Edit Pokemon{% endblock %}

{% block body %}

<div class="card mt-5 ml-3 mr-3">
    <h1>Edit Pokemon</h1>
</div>

    {{ include('pokemon/_form.html.twig') }}

    <div class="row">
    <a class = "btn btn-primary mr-2 ml-4" href="{{ path('app_pokemon_index') }}">back to list</a>

    {{ include('pokemon/_delete_form.html.twig') }}</div>
{% endblock %}
