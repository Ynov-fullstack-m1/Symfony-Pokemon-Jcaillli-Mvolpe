{% extends 'base.html.twig' %}

{% block title %}Edit Pokemon{% endblock %}

{% block body %}
    <h1 class="text-center">Edit Pokemon : {{ pokemon.nom }}</h1>

    <div class="card">

            {{ include('pokemon/_form.html.twig') }}

                <a class = "btn btn-secondary mr-2" href="{{ path('app_pokemon_index') }}">back to list</a>

                {{ include('pokemon/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
